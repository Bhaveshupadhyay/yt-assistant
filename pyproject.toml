[project]
name = "yt-assistant"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "backend",
]

[tool.uv.sources]
backend = { workspace = true }

[tool.uv.workspace]
members = [
    "backend",
]
